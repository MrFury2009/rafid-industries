# Vercel KV — server-side only, never NEXT_PUBLIC_
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Admin auth — server-side only
ADMIN_PASSWORD=

# Do NOT prefix any of the above with NEXT_PUBLIC_
