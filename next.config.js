/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // No experimental flags needed for Next.js 14 App Router
  },
  images: {
    remotePatterns: [],
  },
}

module.exports = nextConfig
